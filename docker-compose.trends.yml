services:
  trends-worker:
    build:
      context: .
      dockerfile: Dockerfile.trends
    env_file: ${KATCHA_ENV_FILE:-.env}
    depends_on:
      migrate:
        condition: service_completed_successfully
      temporal:
        condition: service_started
    command: ["python", "-m", "katcha.orchestration.trend_worker"]
